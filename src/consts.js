import { firebaseEnv as firebase, gitHubEnv as github, uiGA as gAnalytics } from '../.env.local';

export const firebaseEnv = firebase;
export const gitHubEnv = github;
export const uiGA = gAnalytics;
